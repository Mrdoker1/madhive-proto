workflow:
  name: MadHive build for $CI_COMMIT_BRANCH $CI_COMMIT_TAG $CI_MERGE_REQUEST_SOURCE_BRANCH_NAME
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
    - if: $CI_PIPELINE_SOURCE == "web"
    - if: $CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_BRANCH =~ /dev|main/
    - if: $CI_COMMIT_BRANCH && $CI_OPEN_MERGE_REQUESTS
      when: never
    - if: $CI_COMMIT_BRANCH

stages:
  - init
  - build
  - deploy

variables:
  DOCKER_DRIVER: overlay2
  DOCKER_TLS_CERTDIR: ""
  DOCKER_HOST: tcp://docker:2375
  DOCKER_BUILDKIT: 1
  DEPLOY_HOST: 192.168.113.250

default:
  tags:
    - oxaid
  interruptible: true

init:
  stage: init
  image: alpine:3.22
  script:
    - apk add curl bash
    - curl --silent "https://gitlab.com/gitlab-org/incubation-engineering/mobile-devops/download-secure-files/-/raw/main/installer" | bash
    # Copy SSL certificate and key required for nginx https
    - cp .secure_files/oxagile* deploy/ssl
    - echo WEB_IMAGE=${HARBOR_HOST}/${HARBOR_PROJECT}/madhive:${CI_COMMIT_REF_SLUG}-$(date +'%d-%m-%Y-%H-%M') >> build.env
  artifacts:
    reports:
      dotenv: build.env
  rules:
    - if: $CI_PIPELINE_SOURCE == "web" || $CI_PIPELINE_SOURCE == "merge_request_event"
    - if: $CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_BRANCH =~ /dev|main/

build:
  stage: build
  image: docker:27
  variables:
    GIT_CLEAN_FLAGS: none
    CACHE_IMAGE: "${HARBOR_HOST}/${HARBOR_PROJECT}/madhive:cache"
  services:
    - name: docker:27-dind
      alias: docker
  before_script:
    - echo "$HARBOR_PASSWORD" | docker login $HARBOR_URL -u $HARBOR_USERNAME --password-stdin
    - docker buildx create --name container --driver docker-container --driver-opt default-load=true docker
    - docker buildx inspect container --bootstrap
  script:
    - |
      docker buildx build \
      --cache-from type=registry,ref=$CACHE_IMAGE \
      --cache-to type=registry,ref=$CACHE_IMAGE,mode=max \
      --tag $WEB_IMAGE \
      --push \
      --builder container \
      --output type=image .
  rules:
    - if: $CI_PIPELINE_SOURCE == "web" || $CI_PIPELINE_SOURCE == "merge_request_event"
    - if: $CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_BRANCH =~ /dev|main/

deploy:
  stage: deploy
  image: docker:27
  variables:
    GIT_CLEAN_FLAGS: none
  services:
    - name: docker:27-dind
      alias: docker
  before_script:
    - 'command -v ssh-agent >/dev/null || ( apt-get update -y && apt-get install openssh-client -y )'
    - eval $(ssh-agent -s)
    - chmod 400 "$SSH_PRIVATE_KEY"
    - ssh-add "$SSH_PRIVATE_KEY"
    - mkdir -p ~/.ssh
    - chmod 700 ~/.ssh
    - cp "$SSH_KNOWN_HOSTS" ~/.ssh/known_hosts
    - chmod 644 ~/.ssh/known_hosts
    - echo "$HARBOR_PASSWORD" | docker login $HARBOR_URL -u $HARBOR_USERNAME --password-stdin
  script:
    - docker context create remote --docker host=ssh://$DEPLOY_HOST
    - docker --context remote compose up -d
  rules:
    - if: $CI_PIPELINE_SOURCE == "web"
    - if: $CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_BRANCH =~ /dev|main/

