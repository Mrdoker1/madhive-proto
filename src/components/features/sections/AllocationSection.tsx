import { AllocationSection as AllocationSectionComponent } from './allocation';

export const AllocationSection: React.FC = (props) => {
  return <AllocationSectionComponent {...props} />;
};

export default AllocationSection;
